<nav class="navbar navbar-expand-lg navbar-light bg-info fixed-top">
  <%= link_to "AquaList's", aquaria_path, class: "navbar-brand title-logo" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= header_link_item('トップ', root_path) %>
      </li>
      <li class="nav-item">
        <%= header_link_item('水族館一覧', aquaria_path) %>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          ユーザー
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% if @current_user %>
          <%= link_to 'マイページ', user_path(@current_user), class: 'dropdown-item' %>
          <%= link_to 'ログアウト', logout_path, method: :delete, class: 'dropdown-item' %>
          <% else %>
          <%= link_to '登録', new_user_path, class: 'dropdown-item' %>
          <%= link_to 'ログイン', homes_path, class: 'dropdown-item' %>
          <% end %>
        </div>
      </li>
    </ul>
    <%
=begin%>
    <% if @current_user.administrator? %>
    <%= form_with url: administrator_aquaria_path, method: :get, class: "form-inline my-2 my-lg-0", local: true do |form| %>
    <%= form.text_field :search, class: "form-control mr-sm-2" %>
    <%= form.submit '検索', name: nil, class: "btn btn-outline-light my-2 my-sm-0" %>
    <% end %>
    <% else %>
    <%
=end%>
    <%= form_with url: aquaria_path, method: :get, class: "form-inline my-2 my-lg-0", local: true do |form| %>
    <%= form.text_field :search, class: "form-control mr-sm-2" %>
    <%= form.submit '検索', name: nil, class: "btn btn-outline-light my-2 my-sm-0" %>
    <% end %>
    <%
=begin%>
    <% end %>
    <%
=end%>
  </div>
</nav>